package data

/*

import (
	. "github.com/smartystreets/goconvey/convey"
)

func TestUsage(t *testing.T) {
	Convey("Should Get/Add/Remove Usage", t, func() {
		ctx := context.Background()
		key := "test_usage"
		count := Count(ctx, key, time.Duration(24)*time.Hour)
		So(count, ShouldEqual, 0)
	})
}
*/
